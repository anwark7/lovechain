# Hardhat + hardhat-toolbox occasionally trip over pnpm's symlinked node_modules
# when resolving Solidity plugin peer deps. A hoisted linker sidesteps that and
# is the documented workaround for Hardhat under pnpm.
node-linker=hoisted
